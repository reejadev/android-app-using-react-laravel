# My Awesome Project

This repository contains both the mobile application and the backend API.

## Project Structure
- **/app**: React Native (Expo) frontend.
- **/api**: Laravel PHP backend.

## Setup
1. Go to `/api`, run `composer install` and `php artisan migrate`.
2. Go to `/app`, run `npm install` and `npx expo start`.
